@if (city is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <CityForm @ref="cityForm" City="city" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}